// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // GitHub Organization: snu-mlsc, repo: snu-mlsc.github.io
  site: 'https://snu-mlsc.github.io',
  build: { format: 'directory' },
});
